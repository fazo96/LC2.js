; You can execute the run_program.js script to assemble then run this program
; in the emulator. This program is just some random instructions for debugging
; purposes. In the future, simple but actually purposeful programs will be
; included with this emulator

add r0, r0, #4
add r1, r1, #2
add r2, r0, r1
st r2, #4
